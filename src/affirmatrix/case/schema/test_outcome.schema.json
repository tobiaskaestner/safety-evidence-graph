{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://affirmatrix.dev/case/schema/test_outcome.schema.json",
  "title": "A TestOutcome node record",
  "description": "A TestOutcome node record. The digest covers the recorded result of one execution of one specification.",
  "type": "object",
  "required": [
    "id",
    "type",
    "seg:localId",
    "seg:contentHash"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^https?://",
      "description": "An absolute IRI minted at serialization time. Opaque: its parts are never split back out, because a part may itself contain the separator (ADR-0007)."
    },
    "type": {
      "const": "seg:TestOutcome"
    },
    "seg:localId": {
      "type": "string",
      "minLength": 1,
      "description": "The case-local stable identifier — the string that enters a hash preimage. The id above is its serialization, not a second identity."
    },
    "seg:contentHash": {
      "$ref": "#/$defs/sha256"
    }
  },
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "A SHA-256 digest, lowercase hex, exactly 64 characters. SHA-256 is fixed for the whole graph, so the digest needs no algorithm beside it (ADR-0005)."
    }
  }
}
