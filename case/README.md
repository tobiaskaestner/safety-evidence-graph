# case/ — the dogfood evidence graph

This directory will hold affirmatrix's own safety evidence graph (nodes,
edges, events, proofs) once the engine lands: the repo's requirement and
test-specification documents, source markers, and test reports, sealed by
the tool itself. Kept outside `doc/` — it is an artifact store, not a
document (DEC-030).
