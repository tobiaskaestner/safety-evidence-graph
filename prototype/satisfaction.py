# Leaf/non-leaf recursive requirement coverage per DEC-001.
# A non-leaf requirement is satisfied iff every child subtree is satisfied.
# A leaf requirement is satisfied iff at least one active verifies edge reaches a PASS outcome.
