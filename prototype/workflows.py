# The three core workflows — callable functions, not a self-asserting test suite.
#
# consistency()          — structural gaps, link states, requirement satisfaction
# generate_proof(scope)  — proof over a requirement scope + partial-vs-total signal (DEC-002)
# detect_suspect()       — recompute after a store mutation; report changed link-states
