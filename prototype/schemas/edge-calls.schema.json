{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://zephyrproject.org/seg/schema/edge-calls.schema.json",
  "title": "SEG Calls Edge",
  "description": "Schema for a Calls edge in the Safety Evidence Graph. Direction: Implementation → Implementation (caller → callee). RESERVED FOR FUTURE USE. Not implemented in any extractor, gate, notifier, or proof step. Carries no suspicion propagation, no edgeHash, no linkState, no Merkle participation. Retained in the taxonomy as a placeholder for potential future call-graph analysis. The corresponding calls.jsonld instance file remains empty.",

  "allOf": [
    { "$ref": "edge-base.schema.json#/$defs/baseEdge" }
  ],

  "properties": {

    "type": {
      "const": "seg:Calls"
    },

    "id": {
      "pattern": "^https://[^/]+/[^/]+/edge/calls/[a-zA-Z_][a-zA-Z0-9_]*-[a-zA-Z_][a-zA-Z0-9_]*$",
      "description": "Form: {namespaceBase}/edge/calls/{callerFunctionName}-{calleeFunctionName}."
    },

    "seg:from": {
      "pattern": "^https://[^/]+/[^/]+/impl/[a-zA-Z_][a-zA-Z0-9_]*$",
      "description": "IRI of the calling Implementation node."
    },

    "seg:to": {
      "pattern": "^https://[^/]+/[^/]+/impl/[a-zA-Z_][a-zA-Z0-9_]*$",
      "description": "IRI of the called Implementation node."
    }
  },

  "unevaluatedProperties": false
}
